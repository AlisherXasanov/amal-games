/** Чат класса — включи после создания Firebase (бесплатно). */
window.AMAL_CHAT_CONFIG = {
  enabled: false,
  databaseURL: "",
  // Пример когда настроишь:
  // enabled: true,
  // databaseURL: "https://ТВОЙ-ПРОЕКТ-default-rtdb.firebaseio.com",
};
